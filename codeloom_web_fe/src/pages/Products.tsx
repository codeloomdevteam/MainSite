import { PartyCrafter, ProductList, Title } from "../features/Product";

const Products = () => {
  return (
    <div>
      <Title />
      <ProductList />
      <PartyCrafter />
    </div>
  );
};

export default Products;
